import React from 'react';

function NoMatch() {
    return (
        <div>
            <h1>AH AH AH AH ! Tu t'es trompé de page ! (C'est pas la bonne)</h1>

            <p>
                Ca s'appelle une erreur 404, mais c'est pas grave (en fait SI), tu peux retourner à
                la page d'accueil.
            </p>
        </div>
    );
}

export default NoMatch;
